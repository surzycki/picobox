version: '2'

services:
  dev:
    build: .
    entrypoint: .picobox/start
    volumes:
      - .:/etc/ansible
      - $SSH_AUTH_SOCK:$SSH_AUTH_SOCK
    environment:
      SSH_AUTH_SOCK: $SSH_AUTH_SOCK
    hostname: picobox
