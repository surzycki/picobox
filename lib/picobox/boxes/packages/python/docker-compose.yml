version: '2'
volumes:
  db-data:
    external: false

services:
  dev:
    build: .
    entrypoint: ".picobox/start"
    environment:
      SSH_AUTH_SOCK: $SSH_AUTH_SOCK
    hostname: picobox
    volumes:
    - ".:/~:cached"